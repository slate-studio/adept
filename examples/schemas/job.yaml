userId:
  type:     string
  required: true

type:
  type:     string
  required: true

state:
  $ref:    'JobState'
  default: pending

parameters:
  type: object

debug:
  type: object

error:
  type: object
